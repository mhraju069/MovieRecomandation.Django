apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "popn.fullname" . }}-test-connection"
  labels:
    {{- include "popn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "popn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
